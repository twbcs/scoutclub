<%= form_for [:dashboard,:admin,art_kind] do |f| %>
  <%= render 'shared/errors', object: f.object %>
  <%= f.text_field :name, class: "form-control", placeholder: "名稱" %>
  <%= f.submit "確認", disable_with: '處理中...', class: "btn btn-success" %>
<% end %>
