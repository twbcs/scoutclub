<ol class="breadcrumb">
  <li><%= link_to "首頁", dashboard_root_path %></li>
  <li class="active">團康音樂</li>
</ol>

<% @musics.each do |m|%>
  <%= link_to m.title, dashboard_music_path(m.id) %>
  <%= File.basename(m.file.path) %>
  <%= m.user.name %>
<% end %>
