<div class="col-md-6">
  <div id="training" class="list">
    <table class="table table-hover">
      <thead>
        <tr>
          <td>
            <strong class="ui teal ribbon label">近期授課</strong>
          </td>
          <td align="right">
            <%= link_to "更多...", schedules_path(doing_kind_id: 2) %>
          </td>
        </tr>
      </thead>
      <tbody>
        <% @training_list.each do |train| %>
        <tr>
          <td><%= link_to train.title, schedule_path(train.id) %></td>
          <td align="right">
            <%= '還有' if train.start_date > Time.now %>
            <%= timeago_tag train.start_date, nojs: true, limit: 30.days.ago, lang: 'zh-TW' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<div class="col-md-6">
  <div id="schedules" class="list">
    <table class="table table-hover">
      <thead>
        <tr>
          <td>
            <strong class="ui teal ribbon label">近期活動</strong>
          </td>
          <td align="right">
            <%= link_to "更多...", schedules_path %>
          </td>
        </tr>
      </thead>
      <tbody>
        <% @schedule_list.each do |doing| %>
        <tr>
          <td><%= link_to doing.title, schedule_path(doing.id) %></td>
          <td align="right">
            <%= '還有' if doing.start_date > Time.now %>
            <%= timeago_tag doing.start_date, nojs: true, limit: 30.days.ago, lang: 'zh-TW' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<div class="col-md-6">
  <div id="forums" class="list">
    <table class="table table-hover">
      <thead>
        <tr>
          <td>
            <strong class="ui teal ribbon label">討論區最新主題</strong>
          </td>
          <td align="right">
            <%= link_to "更多...", forums_path %>
          </td>
        </tr>
      </thead>
      <tbody>
        <% @post_list.each do |post| %>
        <tr>
          <td><%= link_to post.subject, forum_post_path(forum_id: post.forum_id, id: post.id) %></td>
          <td align="right">
            <%= timeago_tag post.update_post, nojs: true, limit: 30.days.ago, lang: 'zh-TW' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<div class="col-md-6">
  <div id="books" class="list">
    <table class="table table-hover">
      <thead>
        <tr>
          <td>
            <strong class="ui teal ribbon label">近期文章</strong>
          </td>
          <td align="right">
            <%= link_to "更多...", articles_path %>
          </td>
        </tr>
      </thead>
      <tbody>
        <% @articles_list.each do |art| %>
        <tr>
          <td><%= link_to art.title, article_path(art.id) %></td>
          <td align="right">
            <%= timeago_tag art.created_at, nojs: true, limit: 30.days.ago, lang: 'zh-TW' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<div class="col-md-6">
  <div id="photos" class="list">
    <table class="table table-hover">
      <thead>
        <tr>
          <td>
            <strong class="ui teal ribbon label">最新相簿</strong>
          </td>
          <td align="right">
            <%= link_to "更多...", albums_path %>
          </td>
        </tr>
      </thead>
      <tbody>
        <% @album_list.each do |album| %>
        <tr>
          <td><%= link_to album.title, album_path(album.id) %></td>
          <td align="right">
            <%= timeago_tag album.created_at, nojs: true, limit: 30.days.ago, lang: 'zh-TW' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
<div class="col-md-6">
  <div id="movies" class="list">
    <table class="table table-hover">
      <thead>
        <tr>
          <td>
            <strong class="ui teal ribbon label">近期影片</strong>
          </td>
          <td align="right">
            <%= link_to "更多...", movies_path %>
          </td>
        </tr>
      </thead>
      <tbody>
        <% @movie_list.each do |movie| %>
        <tr>
          <td><%= link_to movie.title, movie_path(movie.id) %></td>
          <td align="right">
            <%= timeago_tag movie.created_at, nojs: true, limit: 30.days.ago, lang: 'zh-TW' %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
