<ol class="breadcrumb">
  <li><%= link_to "首頁", root_path %></li>
  <li class="active">活動行事曆</li>
</ol>
<% @schedules.each do |t| %>
  <%= t.title %>
  <%= t.start_date.strftime("%Y %m %d %H:%M") %>
  <%= t.end_date.strftime("%Y %m %d %H:%M") %>
  <%= t.public_at.strftime("%Y %m %d %H:%M") if t.public_at %>
  <%= t.local %>
  <%= t.number %>
  <%= t.description %>
  <%= t.kind_title %><br/>
<% end %>
